#include <bits/stdc++.h>

using namespace std;

int main () {

    unsigned int n, k;
    list<unsigned int> l;
    list<unsigned int>::iterator it;

    cin >> n >> k;

    for (int i=0; i < l.size(); i++) {
        l.push_back(i);
    }

    it = l.begin();
    while(l.size() != 1) {
    
    }

    return 0;
}